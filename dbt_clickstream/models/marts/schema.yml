version: 2

models:
  - name: fct_conversion_funnel
    description: "Segment-level fact model (device, variant, acquisition channel, user type) tracking funnel progression, drop-offs, and A/B variant conversions"
    columns:
      - name: variant
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['control', 'new_checkout_ui']
      
      - name: device
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['mobile', 'desktop', 'tablet']

      - name: acquisition_channel
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['Organic', 'Google_Ads', 'Email', 'Direct']

      - name: user_type
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['new_user', 'returning_user']
      
      - name: total_sessions
        data_tests:
          - not_null
      
      - name: unique_users
        data_tests:
          - not_null

      - name: overall_conversion_rate
        description: "End-to-end purchase conversion rate percentage."
        data_tests:
          - not_null

  - name: fct_ab_test_summary
    description: "Experiment-level summary table aggregating total sample sizes, stage volumes, and baseline conversion rates across A/B test variants for hypothesis testing."
    columns:
      - name: variant
        description: "A/B test assignment group."
        data_tests:
          - not_null
          - unique
          - accepted_values:
              arguments:
                values: ['control', 'new_checkout_ui']

      - name: total_sessions
        description: "Total session sample size (N_sessions)."
        data_tests:
          - not_null

      - name: total_users
        description: "Total user sample size (N_users)."
        data_tests:
          - not_null

      - name: total_conversions
        description: "Total successful purchase conversions (K)."
        data_tests:
          - not_null

      - name: overall_conversion_rate
        description: "Proportion of converted sessions relative to total sessions (p_hat)."
        data_tests:
          - not_null

      - name: checkout_to_purchase_rate
        description: "Proportion of sessions reaching checkout that completed purchase."
        data_tests:
          - not_null
  
  - name: fct_user_retention
    description: "Cohort retention breakdown by signup date, variant, and acquisition channel."
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - cohort_date
              - variant
              - acquisition_channel
    columns:
      - name: cohort_date
        data_tests:
          - not_null
      - name: variant
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['control', 'new_checkout_ui']
      - name: cohort_size
        data_tests:
          - not_null
      - name: d1_retention_rate
        data_tests:
          - not_null

  - name: fct_ab_transitions
    description: "Average time (seconds) to advance between each funnel step, broken down at both the segment level (variant, device, acquisition_channel, user_type) and the variant level alone, for comparing checkout-UI speed independent of the conversion-rate result."
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - variant
              - device
              - acquisition_channel
              - user_type
    columns:
      - name: variant
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['control', 'new_checkout_ui']

      - name: grain
        description: "'segment_level' for device/channel/user_type breakdowns, 'variant_level' for the variant-only rollup (device/acquisition_channel/user_type are null on those rows by design)."
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['segment_level', 'variant_level']

      - name: device
        data_tests:
          - accepted_values:
              arguments:
                values: ['mobile', 'desktop', 'tablet']
              config:
                where: "grain = 'segment_level'"

      - name: acquisition_channel
        data_tests:
          - accepted_values:
              arguments:
                values: ['Organic', 'Google_Ads', 'Email', 'Direct']
              config:
                where: "grain = 'segment_level'"

      - name: user_type
        data_tests:
          - accepted_values:
              arguments:
                values: ['new_user', 'returning_user']
              config:
                where: "grain = 'segment_level'"

      - name: total_sessions
        data_tests:
          - not_null

  - name: fct_session_dropoff
    description: "Table details where exactly did the user bounce off in our funnel steps. This information is provided at acquisition channel, devices and variant level  "
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - device
              - variant
              - acquisition_channel
              - exit_event
    columns:
      - name: exit_event
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['view_homepage', 'search_product', 'view_product_details', 'add_to_cart', 'checkout_initiated', 'purchase_completed']

      - name: total_exits
        data_tests:
          - not_null

      - name: bounced_sessions
        data_tests:
          - not_null